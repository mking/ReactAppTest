import { recordSlice, SET_FOO } from '../actions/austriaActions';

const {
  SET_RECORD_LOADING,
  SET_RECORD_ERROR,
  SET_RECORD_REQUEST_ID,
  SET_RECORD_ENTITIES,
} = recordSlice.constants;

export {
  SET_RECORD_LOADING,
  SET_RECORD_ERROR,
  SET_RECORD_REQUEST_ID,
  SET_RECORD_ENTITIES,
  SET_FOO,
};
